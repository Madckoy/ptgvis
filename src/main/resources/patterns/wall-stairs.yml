filters:
  - "x == cx - r"                            # ← одна стена
  - "z >= cz - r && z <= cz + r"
  - "y >= cy - r && y <= cy + r"
  - "z <= cz + r - (cy - y)"                # ← ступенчатое сокращение
